Downstream LLM artifacts (plain text):

- pricing_elements_analysis.txt — free-form explanation of performance-pricing constructs (prompt: docs/prompts/pricing_elements_analysis_prompt.txt; source: score=1 plain hot zone).
- pricing_engine_code.txt — Python pricing engine sketch generated from the same prompt set.

Both files trace back to /tmp/pricing_grid_sample runs captured in docs/prompts.

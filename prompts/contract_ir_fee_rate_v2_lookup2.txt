You are converting credit agreement excerpts into a strictly-validated JSON intermediate representation (ContractIR v0.2).

Return JSON ONLY. No markdown. No commentary. No code fences.

=====================
SCOPE (FEE RATE / GRID NORMALIZATION)
=====================
- Only encode fee rate schedules (facility fee, commitment fee, letter of credit fee, etc.).
- Do NOT encode spreads/margins (semantic_role="spread") or base rates (semantic_role="base_rate").
- Every derived function MUST have semantic_role = "fee_rate".
- This prompt supports normalizing 2D tables into a long table and using lookup2.

=====================
CONTRACTIR v0.2
=====================

Top-level keys (must all be present; use empty arrays when needed):
- schema_version (must be "contract_ir_v0_2")
- contract_id  (MUST equal the item_id from TASK)
- sources
- indices
- tables
- derived
- open_items

cashflows is optional and MAY be omitted.

---------------------
Provenance rules
---------------------
- Every derived function MUST include source_refs (non-empty list of anchor IDs like "A0123").
- Every table row MUST include source_refs (non-empty list of anchor IDs).
- source_refs / anchor_ids must use only the anchor IDs shown in the CONTEXT blocks.

---------------------
Operator allowlist (v0.2)
---------------------
Conversions:
- bps_to_rate(bps)

---------------------
AST grammar (ONLY these 3 node types)
---------------------
1) Literal node:
   {"lit": {"type": "...", "value": ...}}

2) Variable node:
   {"var": "name"}

3) Operator node:
   {"op": "op_name", "args": [<node>, ...]}

IMPORTANT:
- Every value inside any "args" array MUST be an AST node. Do NOT use bare strings or numbers.
- Do NOT invent other AST shapes like {"if": ...} or {"op":"case", ...}. Use ONLY {"op":"if","args":[cond,then,else]}.

Literal types (v0.2):
- "rate" | "bps" | "decimal" | "money" | "integer" | "string" | "bool" | "date"

NUMERIC LITERALS MUST BE STRINGS AND MUST BE PLAIN DECIMALS.
- Do NOT include "%" signs (or "$", commas) inside numeric literal strings.
  - Example: 0.0700% MUST be encoded as {"lit":{"type":"rate","value":"0.0007"}} (not "0.0700%").

Tables:
- lookup(table_id, key_column, key_value, value_column)
- lookup2(table_id, key1_column, key1_value, key2_column, key2_value, value_column)

Table argument encoding (IMPORTANT):
- table_id, key_column(s), and value_column MUST be AST string literal nodes:
  - {"lit":{"type":"string","value":"..."}}
- key_value args are usually variable nodes, e.g. {"var":"status_code"}.

---------------------
Common fee schedule pattern (discrete status => 1D lookup)
---------------------
If the excerpt says something like:
  “The Facility Fee Rate is: (i) 0.0700% if Level I Status … (ii) 0.0900% if Level II …”
Then:
- Create a table with columns like:
  - status (string)
  - fee_rate (rate)
- And define the fee rate as:
  lookup(table_id, "status", status, "fee_rate")
Do NOT encode this as nested if/case trees unless the excerpt truly requires it.

Conditionals (only if needed):
- if(cond, then_expr, else_expr)
- eq/lt/lte/gt/gte
- and/or/not

=====================
MISSING DATA POLICY (NO GUESSING)
=====================
- If the TASK requires rows/cells that are NOT explicitly present in the provided CONTEXT, DO NOT GUESS values.
- Instead: emit a blocking open_item describing what is missing and set:
  - tables = [] (empty)
  - derived = [] (empty)

---------------------
OPEN_ITEMS schema reminder (v0.2)
---------------------
Each open_items entry MUST be an object with:
- issue (string)
- source_refs (array of anchor ids; non-empty)
- suggested_parameters (array of strings; required; can be empty)
- blocking (boolean)

=====================
OUTPUT SHAPE (must match exactly)
=====================
{
  "schema_version": "contract_ir_v0_2",
  "contract_id": "<string>",
  "sources": [
    {
      "source_id": "S1",
      "kind": "excerpt_pack",
      "item_id": "<item_id>",
      "anchor_ids": ["A0001"],
      "notes": null
    }
  ],
  "indices": [],
  "tables": [
    {
      "table_id": "<string>",
      "description": null,
      "columns": [{"name":"...", "type":"string|decimal|rate|bps|money|bool|date"}],
      "rows": [
        {"row_id": null, "cells": {"col": {"lit": {"type": "...", "value": "..."}}}, "source_refs": ["A0001"]}
      ]
    }
  ],
  "derived": [
    {
      "fn_id": "<string>",
      "semantic_role": "fee_rate",
      "description": null,
      "args": [{"name":"...", "type":"string|decimal|rate|bps|money|bool|date"}],
      "returns": "string|decimal|rate|bps|money|bool|date",
      "expr": {"op":"...", "args":[...]},
      "source_refs": ["A0001"]
    }
  ],
  "open_items": []
}

=====================
TASK
=====================
{{TASK}}

=====================
CONTEXT
=====================
{{CONTEXTS}}

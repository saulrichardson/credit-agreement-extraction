You are reading a segment of a loan agreement.
Each line begins with an anchor like [[A0123]].

List every anchor whose text contains a formal definition of a financial term.

Include anchors that contain:
- Definitions starting with "means", "shall mean", or "is defined as"
- Definitions of leverage ratios, coverage ratios, EBITDA, debt terms, fees tied to usage, and other capitalized terms followed by their definition

Return JSON only:
{
  "anchors": ["A0123", "A0456", ...]
}

Output only valid JSON. Do not include explanations or text outside the JSON.

The document now follows below.

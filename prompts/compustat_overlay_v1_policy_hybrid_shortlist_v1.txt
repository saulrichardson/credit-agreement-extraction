You are a financial data analyst.

Return JSON only. Do not add commentary.

=== TASK ===
You are given ONE credit-agreement metric/term (with its verbatim contract definition already extracted),
plus a best-effort calculation AST (may be null) and the list of other defined-term inputs (may be empty).

Your job is to produce a best-effort mapping from the term to a Compustat-style formula using a fixed allowlist
of quarterly Compustat variables.

This mapping is an interpretation step (not a verbatim extraction). It may be exact, approximate, or impossible.

=== ALLOWLIST SHORTLIST (READ FIRST) ===
These are common building-block variables that DO exist in the allowlist table below:
- oibdpq: Operating Income Before Depreciation – Quarterly
- niq: Net Income (Loss)
- ibq: Income Before Extraordinary Items
- dpq: Depreciation and Amortization – Total
- xintq: Interest and Related Expense- Total
- txtq: Income Taxes – Total
- txpdy: Income Taxes Paid
- capxy: Capital Expenditures
- dvy: Cash Dividends
- dltry: Long-Term Debt – Reduction
- dlcq: Debt in Current Liabilities
- dlttq: Long-Term Debt – Total
- dlcchy: Changes in Current Debt
- xiq: Extraordinary Items
- miiq: Minority Interest – Income Account
- fcaq: Foreign Exchange Income (Loss)
- saleq: Sales/Turnover (Net)
- cogsq: Cost of Goods Sold
- xsgaq: Selling, General and Administrative Expense

Do NOT assume any other codes exist unless you see them in the allowlist table.

=== COMPUSTAT VARIABLE ALLOWLIST (QUARTERLY) ===
Use ONLY these codes if you provide a Compustat mapping:

{{COMPUSTAT_ALLOWLIST_TABLE}}

=== DECISION POLICY (MUST FOLLOW) ===
There can be multiple reasonable mappings. You MUST:
1) Choose exactly ONE primary mapping (compustat_formula + compustat_variables).
2) Record at least ONE alternative mapping in notes[] using the required "alternative:" format.

Primary mapping preference: POLICY_HYBRID_SHORTLIST_V1

Step 1: use the AST when available
- If EXPRESSION_AST is not null and it is a simple arithmetic structure (e.g., a ratio of defined terms),
  you MUST use it as the primary skeleton for compustat_formula.

Step 2: explicit proxy rule for EBITDA
- If TERM is "EBITDA": set compustat_formula to "oibdpq" as the primary mapping.
  - Rationale: it is the closest single-variable EBITDA-like proxy in the allowlist.
  - You MUST still set match_type="approximate" unless the contract definition is trivially identical.
  - You MUST include at least one alternative formula in notes[] showing a net-income-based approach
    (e.g., niq + dpq + xintq + txtq or similar), and explain why you did not choose it as primary.

Step 3: avoid over-confidence on approximations
- If match_type is "approximate", confidence MUST be "medium" or "low" (never "high").

Guardrail: avoid industry-specific variables by default
- Treat any allowlist variable whose DESCRIPTION contains "Financial Services" as DISALLOWED unless the contract definition
  clearly indicates a financial institution / financial services context.
- If you still use such a variable, you MUST set confidence="low" and include an explicit "assumption:" explaining why.

Notes formatting (machine-auditable; MUST FOLLOW):
- notes[] MUST include:
  - 1+ entries starting with "assumption:"
  - 1+ entries starting with "alternative: formula=<...>; variables=[...]; why=<...>"
- limitations[] MUST be plain statements (do NOT start limitations with "assumption:"/"alternative:"/"next_step:").

=== RULES ===
1) Your output MUST be JSON and MUST follow the schema below exactly (no extra keys).
2) compustat_formula may be null (if no mapping).
3) If compustat_formula is not null:
   - It MUST use only codes from the allowlist and operators + - * / ( ) . and whitespace.
   - compustat_variables MUST list exactly the codes used in compustat_formula (deduped).
4) match_type:
   - "exact" if the Compustat mapping matches the contract definition with no material omissions.
   - "approximate" if it is a reasonable proxy but ignores material adjustments/defined-term inputs.
   - "none" if a Compustat mapping is not possible from the allowlist.
5) confidence:
   - "high" / "medium" / "low" for how confident you are that the mapping is appropriate.
6) limitations[] should list concrete limitations when match_type is "approximate" or "none".
   - If the contract definition depends on INPUT_TERMS that are NOT represented in compustat_formula, list them explicitly.
   - If you ignore contract clauses/adjustments (e.g., pro forma, exclusions, restricted/unrestricted, secured/unsecured), say so explicitly.
7) notes[] should capture decision process in a machine-auditable way:
   - Include 1+ strings starting with "assumption:" for any assumption you make.
   - If multiple variables are plausible matches, include 1+ "alternative:" strings describing the other candidate(s).
   - If match_type is "none", include 1+ strings starting with "next_step:" describing what additional data/variable(s) would be needed.

=== OUTPUT JSON SCHEMA (EXACT SHAPE) ===
{
  "schema_version": "compustat_overlay_v1",
  "term": "string",
  "compustat_formula": "string | null",
  "compustat_variables": ["string"],
  "match_type": "exact" | "approximate" | "none",
  "confidence": "high" | "medium" | "low",
  "limitations": ["string"],
  "notes": ["string"]
}

=== INPUT ===
TERM: {{TERM}}

DEFINITION_VERBATIM:
{{DEFINITION_VERBATIM}}

EXPRESSION_AST (may be null):
{{EXPRESSION_AST_JSON}}

INPUT_TERMS (defined-term numeric inputs, may be empty):
{{INPUT_TERMS_JSON}}

CLAUSES (verbatim computation clauses, may be empty):
{{CLAUSES_JSON}}

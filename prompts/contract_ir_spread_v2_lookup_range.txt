You are converting credit agreement excerpts into a strictly-validated JSON intermediate representation (ContractIR v0.2).

Return JSON ONLY. No markdown. No commentary. No code fences.

=====================
SCOPE (SPREAD / RANGE SCHEDULES)
=====================
- Only encode pricing spreads / margins / pricing schedules that are keyed by a numeric input (e.g., leverage ratio, net income).
- Do NOT encode fees (semantic_role="fee_rate") or base rates (semantic_role="base_rate").
- Every derived function MUST have semantic_role = "spread".

=====================
CONTRACTIR v0.2
=====================

Top-level keys (must all be present; use empty arrays when needed):
- schema_version (must be "contract_ir_v0_2")
- contract_id  (MUST equal the item_id from TASK)
- sources
- indices
- tables
- derived
- open_items

cashflows is optional and MAY be omitted.

---------------------
Provenance rules
---------------------
- Every derived function MUST include source_refs (non-empty list of anchor IDs like "A0123").
- Every table row MUST include source_refs (non-empty list of anchor IDs).
- source_refs / anchor_ids must use only the anchor IDs shown in the CONTEXT blocks.

---------------------
Semantic role (derived functions)
---------------------
For this pass: semantic_role MUST be "spread".

---------------------
AST grammar (ONLY these 3 node types)
---------------------
1) Literal node:
   {"lit": {"type": "...", "value": ...}}

2) Variable node:
   {"var": "name"}

3) Operator node:
   {"op": "op_name", "args": [<node>, ...]}

Literal types (v0.2):
- "rate" | "bps" | "decimal" | "money" | "integer" | "string" | "bool" | "date"

NUMERIC LITERALS MUST BE STRINGS.

---------------------
Operator allowlist (v0.2)
---------------------
Conversions:
- bps_to_rate(bps)

Tables:
- lookup_range(table_id, key_value, lower_bound_col, lower_cmp_col, upper_bound_col, upper_cmp_col, value_col)
  - Matches EXACTLY ONE row where:
    - if lower bound is present: key_value > lower_bound (gt) or >= (gte)
    - if upper bound is present: key_value < upper_bound (lt) or <= (lte)
  - lower_cmp must be "gt" or "gte"
  - upper_cmp must be "lt" or "lte"
  - table_id and column name args MUST be AST string literal nodes:
    - {"lit":{"type":"string","value":"..."}}

=====================
RANGE TABLE ENCODING RULES
=====================
- Use a table to represent the schedule. Each row represents ONE bucket/tier.
- Each row MUST include:
  - bucket_label (string): exact bucket label text from the excerpt
  - lower_bound (decimal or money): numeric lower bound (omit the cell if unbounded)
  - lower_cmp (string): "gt" or "gte" (omit the cell if lower_bound is omitted)
  - upper_bound (decimal or money): numeric upper bound (omit the cell if unbounded)
  - upper_cmp (string): "lt" or "lte" (omit the cell if upper_bound is omitted)
  - spread_bps (bps) OR spread_rate (rate), as specified by the TASK

=====================
MISSING / AMBIGUOUS DATA POLICY
=====================
- If a bucket boundary is ambiguous in the excerpt, do NOT guess:
  - Emit a blocking open_item describing the ambiguity.
  - Return tables=[] and derived=[].

---------------------
OPEN_ITEMS schema reminder (v0.2)
---------------------
Each open_items entry MUST be an object with:
- issue (string)
- source_refs (array of anchor ids; non-empty)
- suggested_parameters (array of strings; required; can be empty)
- blocking (boolean)

=====================
OUTPUT SHAPE (must match exactly)
=====================
{
  "schema_version": "contract_ir_v0_2",
  "contract_id": "<string>",
  "sources": [
    {
      "source_id": "S1",
      "kind": "excerpt_pack",
      "item_id": "<item_id>",
      "anchor_ids": ["A0001"],
      "notes": null
    }
  ],
  "indices": [],
  "tables": [
    {
      "table_id": "<string>",
      "description": null,
      "columns": [{"name":"...", "type":"string|decimal|rate|bps|money|bool|date"}],
      "rows": [
        {"row_id": null, "cells": {"col": {"lit": {"type": "...", "value": "..."}}}, "source_refs": ["A0001"]}
      ]
    }
  ],
  "derived": [
    {
      "fn_id": "<string>",
      "semantic_role": "spread",
      "description": null,
      "args": [{"name":"...", "type":"string|decimal|rate|bps|money|bool|date"}],
      "returns": "string|decimal|rate|bps|money|bool|date",
      "expr": {"op":"...", "args":[...]},
      "source_refs": ["A0001"]
    }
  ],
  "open_items": []
}

=====================
TASK
=====================
{{TASK}}

=====================
CONTEXT
=====================
{{CONTEXTS}}

You are extracting exact definition language from a credit agreement.
Return JSON only. Do not add commentary.

=== TASK ===
Given a TERM and multiple CONTEXT blocks from the agreement, output the exact
definition wording for that term, if it is explicitly defined.

The term may be a:
- metric (e.g., "Consolidated Secured Net Leverage Ratio")
- rate term (e.g., "Base Rate", "Adjusted Term SOFR", "Eurocurrency Rate")

=== RULES ===
1) Use ONLY the provided CONTEXT blocks.
2) Copy the definition wording exactly as it appears in the agreement.
3) If the term is not explicitly defined in the contexts, return null for
   definition_text and include a short note explaining that no explicit
   definition was found.
4) If multiple explicit definitions appear, return the best match and list all
   candidate definitions in `candidates`.
5) Anchor refs must be plain anchor IDs like "A0123" (no brackets, no labels).
6) `anchor_refs` must cite ONLY the context block(s) that contain the returned
   `definition_text` (e.g., the block that contains the “<TERM> means …” sentence).
   Do NOT cite blocks that merely mention the term (usage/interest mechanics)
   unless the definition text itself appears in that block.

=== OUTPUT JSON (exact shape) ===
{
  "term": "<term string as provided>",
  "term_type": "metric|rate_term",
  "definition_text": "<exact definition text or null>",
  "anchor_refs": ["A0123", "A0456"],
  "candidates": [
    {
      "definition_text": "<exact definition text>",
      "anchor_refs": ["A0123"]
    }
  ],
  "notes": "<optional short note or null>"
}

=== INPUT ===
TERM: {{TERM}}
TERM_TYPE: {{TERM_TYPE}}

CONTEXTS:
{{CONTEXTS}}

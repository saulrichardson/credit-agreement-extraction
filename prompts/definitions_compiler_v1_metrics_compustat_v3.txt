You are a financial data extraction analyst specializing in credit agreement definitions.

Return JSON only. Do not add commentary.

=== TASK ===
Given ONE financial metric (identified from a performance pricing grid) and multiple anchored context blocks from a credit agreement,
extract the formal definition (verbatim) for that metric (if explicitly defined), and compile it into a structured representation.

You MUST ground every extracted field in the provided context blocks. Do not invent.

=== DEFINITION PATTERNS TO RECOGNIZE ===
Credit agreement definitions typically follow these patterns (examples):
- "Term" means ...
- "Term" shall mean ...
- "Term" is defined as ...
- Term: ...

IMPORTANT: Table headers / row labels are NOT formal definitions.
- If the only evidence is a table label (even if it contains words like "ratio" or "(as a percentage of ...)"), then the metric is NOT explicitly defined.
- In that case set definition_verbatim=null, source_refs=[], confidence="low", and explain in notes.

Definitions often span multiple sentences and may include:
- Mathematical formulas (e.g., "(a) minus (b) plus (c)")
- Adjustments and exclusions (e.g., "excluding extraordinary items")
- References to other defined terms (often Title Case and/or in quotes)
- Temporal specifications (e.g., "for the most recent four fiscal quarters")

When a definition spans multiple context blocks, definition_verbatim MUST include the full definition across those blocks.
Do not truncate mid-sentence if the continuation is present in the provided blocks.

=== COMPUSTAT VARIABLE REFERENCE (ALLOWLIST) ===
Use ONLY these codes if you provide a Compustat approximation:

| Code  | Description                                   |
|-------|-----------------------------------------------|
| dlttq | Long-term debt (total)                        |
| dlcq  | Debt in current liabilities (short-term debt) |
| dd1q  | Long-term debt due in one year                |
| npq   | Notes payable                                 |
| ltq   | Liabilities – total                           |
| xintq | Interest and related expense – total          |
| cheq  | Cash & short-term investments                 |
| actq  | Current assets – total                        |
| lctq  | Current liabilities – total                   |
| revtq | Revenue – total                               |
| oibdpq| Operating income before depreciation (EBITDA) |
| oiadpq| Operating income after depreciation (EBIT)    |
| dpq   | Depreciation and amortization                 |
| niq   | Net income                                    |
| saleq | Sales/turnover (net)                          |
| atq   | Assets – total                                |
| seqq  | Stockholders' equity – parent                 |
| capxq | Capital expenditures                          |
| txtq  | Income taxes – total                          |

Compustat rules:
- formula_compustat MAY be a single code (example: "niq").
- formula_compustat (if not null) MUST only use codes from the allowlist and the operators + - * / ( ) .
- compustat_variables MUST list exactly the codes used in formula_compustat (deduped).

Conservative guidance (avoid over-claiming):
- If formula_compustat is null, requires_non_compustat MUST be true.
- If you can provide a reasonable formula_compustat using allowlisted variables, requires_non_compustat MAY be false.
- If the definition uses other defined terms (e.g., "Consolidated Funded Debt", "EBITDA" as a defined term) or bespoke adjustments/exclusions,
  set requires_non_compustat=true.
  - You MAY still provide a coarse formula_compustat approximation (using allowlisted variables) if there is an obvious nearest Compustat construct,
    but you MUST explain what is being ignored/approximated in non_compustat_reason and/or notes.

Direct mapping examples (ONLY if the definition is plain/GAAP with no bespoke adjustments):
- "Net Income" -> formula_compustat="niq", compustat_variables=["niq"], requires_non_compustat=false
- "Interest Expense" -> formula_compustat="xintq", compustat_variables=["xintq"], requires_non_compustat=false
- "Capital Expenditures" -> formula_compustat="capxq", compustat_variables=["capxq"], requires_non_compustat=false

=== HARD GROUNDING RULES (CRITICAL) ===
1) Use ONLY the provided context blocks.
2) If you provide definition_verbatim, it MUST be copied exactly from the context blocks (no paraphrase).
3) definition_verbatim must come from a definitional sentence/pattern (e.g., 'means', 'shall mean', 'is defined as', or 'Term: ...').
   A table header/label alone is NOT sufficient to set definition_verbatim (use null instead).
4) Every anchor in source_refs MUST be one of the provided anchor IDs.
5) If a field cannot be supported, set it to null (or [] for arrays) and explain briefly in notes.

JSON encoding rule (critical):
- Do NOT place literal line breaks inside JSON strings.
- If the verbatim text contains line breaks, replace them with a single space (do not emit backslash-n sequences).

Parsed-fields grounding:
- Every non-null string in definition_parsed (numerator/denominator/measurement_period) MUST be a verbatim substring of definition_verbatim.
- Every entry in components[] and adjustments[] MUST be a verbatim substring of definition_verbatim.
- Every entry in blocking_terms[] MUST be a verbatim substring of definition_verbatim.

=== BLOCKING_TERMS GUIDANCE (IMPORTANT) ===
Use blocking_terms[] to list OTHER defined/bespoke terms that appear inside the definition_verbatim and would be needed to compute the metric (and therefore block a Compustat mapping).

Rules:
- Each entry should be the defined term itself (short phrase), not an entire sentence or bullet clause.
- Prefer Title Case / quoted defined terms (e.g., "Fixed Charges", "Restricted Payments", "Indebtedness", "EBITDA").
- Do NOT include generic adjustment language like "any extraordinary, unusual, or non-recurring non-cash gains" or "without duplication".
- Do NOT include leading list markers like "-", "(a)", "a)".

=== DEFINITION_PARSED RULES ===
- numerator/denominator are ONLY for ratios.
  - If definition_verbatim explicitly defines a ratio (e.g., 'means the ratio of (i) ... to (ii) ...'), fill numerator and denominator.
  - Otherwise (non-ratio metrics like 'Net Income'), set numerator=null and denominator=null.
- components should list the key defined quantities used in the definition (phrases copied from definition_verbatim).
- adjustments should list explicit add-backs/exclusions/modifiers (phrases copied from definition_verbatim).
- measurement_period should capture explicit period language (e.g., 'for any period', 'for the most recent four fiscal quarters') if present.

=== OUTPUT JSON SCHEMA (EXACT SHAPE) ===
Return one JSON object with exactly these top-level keys:
{
  "metrics": [
    {
      "name": "string",
      "contract_term": "string | null",
      "definition_verbatim": "string | null",
      "definition_parsed": {
        "numerator": "string | null",
        "denominator": "string | null",
        "components": ["string"],
        "adjustments": ["string"],
        "measurement_period": "string | null"
      },
      "formula_compustat": "string | null",
      "compustat_variables": ["string"],
      "requires_non_compustat": true,
      "non_compustat_reason": "string | null",
      "blocking_terms": ["string"],
      "source_refs": ["string"],
      "confidence": "high" | "medium" | "low",
      "notes": ["string"]
    }
  ],
  "unresolved_dependencies": [
    {
      "term": "string",
      "referenced_by": ["string"]
    }
  ]
}

=== OUTPUT RULES ===
- metrics MUST contain exactly ONE object corresponding to the INPUT metric name.
- name MUST exactly match the provided INPUT metric name.
- contract_term should be the defined term as written in the agreement if found (may differ from name). If not found, null.
  - Do NOT include surrounding quote characters in contract_term. Example: use Fixed Charge Coverage Ratio (not "Fixed Charge Coverage Ratio").
- unresolved_dependencies[].referenced_by MUST be the metric name(s) (e.g., ["Fixed Charge Coverage Ratio"]), NOT anchor IDs.
- If the metric is NOT explicitly defined in the contexts:
  - definition_verbatim must be null
  - source_refs must be []
  - confidence must be "low"
  - definition_parsed.numerator must be null
  - definition_parsed.denominator must be null
  - definition_parsed.components must be []
  - definition_parsed.adjustments must be []
  - definition_parsed.measurement_period must be null
  - requires_non_compustat must be true
  - non_compustat_reason should explain that the definition was not found in the provided contexts

=== INPUT ===
METRIC_NAME: {{METRIC_NAME}}
CONTRACT_TERM_HINT: {{CONTRACT_TERM_HINT}}
SEARCH_TOKENS: {{SEARCH_TOKENS}}

CONTEXT BLOCKS (anchored):
{{CONTEXTS}}
